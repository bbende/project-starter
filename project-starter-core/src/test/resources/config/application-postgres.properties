
# Using the {vendor} placeholder wasn't working when using the @AutoConfigureEmbeddedDatabase
# Instead we activate the "postgres" profile and change the flyway locations to include the postgres folder
spring.flyway.locations=classpath:db/migration/base,classpath:db/migration/postgressql,classpath:db/migration/test