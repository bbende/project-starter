
spring.jpa.show-sql=true

# All standard tests with no active profiles specified will use the in-memory H2 DB
spring.flyway.locations=classpath:db/migration/base,classpath:db/migration/h2,classpath:db/migration/test