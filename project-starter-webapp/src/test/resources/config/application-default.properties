# The "default" profile is used when no active profile is specified, for example when
# running an integration test from an IDE and not configuring the test runner.

spring.datasource.type=com.zaxxer.hikari.HikariDataSource
spring.datasource.url=jdbc:h2:mem:project-starter;DB_CLOSE_DELAY=-1
spring.datasource.username=pstarter
spring.datasource.password=